# Copy to secrets.toml and fill in your key.
# This file is safe to commit. secrets.toml is gitignored.
#
# Streamlit Cloud users: add these via the dashboard instead.

GROQ_API_KEY = "gsk_your_key_here"
