# Copy to .env and fill in your keys.
# GROQ_API_KEY is required. TAVILY_API_KEY is optional — omit it to run in
# mock mode (agents deliberate on internal knowledge only, no live search).
GROQ_API_KEY=gsk_your_key_here
TAVILY_API_KEY=tvly_your_key_here
